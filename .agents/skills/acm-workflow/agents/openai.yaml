interface:
  display_name: "ACM Workflow"
  short_description: "Operate ACM practice, plans, tags, Skip, and review"
  default_prompt: "Use $acm-workflow to query this repository's local dashboard, sync status, recommend practice, and record training outcomes."
