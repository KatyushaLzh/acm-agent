# P1001 A+B Problem

Read two signed integers `a` and `b` and print their sum.

## Input

One line containing `a b`.

## Output

One line containing `a+b`.

## Constraints

- `-10^9 <= a,b <= 10^9`.
- The input contains exactly two integers.
