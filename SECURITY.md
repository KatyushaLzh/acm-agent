# Security Policy

## 本地安全边界

ACM Agent 设计为单用户本地工具，只允许监听 `127.0.0.1`。请勿修改为 `0.0.0.0`、通过公网反向代理暴露，或将 `.acm/web-runtime.json` 分享给他人。

本地 API 使用随机临时令牌，并校验 Host、Origin、Content-Type、JSON 结构和请求体大小。运行状态、账号标识、平台缓存、源码和报告均可能包含个人信息，应保留在 `.acm/` 或你的私人解题目录中。

Dashboard 中输入的模型 API Key 只允许出现在经过令牌认证的回环请求体中。持久化分别使用当前用户的 Windows DPAPI、macOS Keychain 或 Linux Secret Service；配置、SQLite、浏览器存储、日志和 API 响应不得包含明文密钥。系统安全存储缺失、未运行或锁定时，保存操作会明确失败，程序不会自动选择文件型 keyring、启动或解锁系统服务，也不会退化为明文或弱加密文件。环境变量只作为当前进程的临时凭据来源。

## 支持范围

维护者优先处理当前版本中的以下问题：

- 绕过回环地址或 API 令牌限制。
- 任意文件读写、路径穿越或源码覆盖。
- 令牌、账号、提交记录或本地路径泄露。
- 恶意题单导致的代码执行或越权访问。

## 报告漏洞

请使用 GitHub 的私有安全报告功能联系维护者。报告中提供受影响版本、复现步骤、影响和建议修复，但不要包含真实访问令牌、账号配置、完整 SQLite 数据库或私人源码。
