# TOC

[TOC]

# 图论

## DFS 序 ST 维护 LCA

- Model: 按 DFS 首次访问序编号，维护每个点父亲在 DFS 序上的最小值，$LCA(u, v)$ 转成区间 $[dfn_u + 1, dfn_v]$ 上父亲 DFS 序最小值查询。
- Invariant / correctness: 设 $dfn_u < dfn_v$，则从 $u$ 到 $v$ 的 DFS 序区间内，深度最低且位于通向 $v$ 分支上的点恰为 $LCA$；若 $u$ 是 $v$ 祖先，跳过 $dfn_u$ 自身即可规避误判。
- Implementation: 先 DFS 求 `dfn/fa/dep`，再对 `fa[dfn[i]]` 建 ST 表，比较函数按父节点的 `dfn` 取较小者。
- Complexity: 预处理 $O(n \log n)$，单次查询 $O(1)$。
- Pitfalls: 区间必须是 `[dfn[u] + 1, dfn[v]]`；根节点父亲哨兵要可比较；该写法依赖 DFS 序性质，不是欧拉序 RMQ 模板。

## Dsu on tree

- Source: `CF600E`
- Model: 轻儿子子树先算后清空，重儿子子树最后算并保留，再把轻子树贡献暴力合并到重子树上。
- Invariant / correctness: 当前节点答案只在“重儿子信息 + 当前节点 + 所有轻子树重新加入”后统计，轻子树的历史信息不会污染兄弟子树答案。
- Implementation: `dfs1` 求 `siz/son`；`dfs2(u, keep)` 先递归轻儿子 `keep=0`，再递归重儿子 `keep=1`，用 `S=son[u]` 跳过重链，`calc` 负责对子树桶加减。
- Complexity: 每个点只会因经过轻边被暴力合并 $O(\log n)$ 次，总复杂度 $O(n \log n)$，常数通常较小。
- Pitfalls: 清空时必须完整回滚桶、当前最大频次和答案和；`calc` 跳过重儿子靠全局 `S`，漏判会重复统计。

## 点分治统计路径

- Model: 树上路径按“经过当前重心”和“完全落在某个子树内部”拆分；前者在当前层统计，后者递归到子树重心。
- Invariant / correctness: 任一路径第一次被统计时对应其经过的最高层重心；对子树逐个处理并在子树间做“先查后插”，可避免同一子树内部路径被误算成跨重心路径。
- Implementation: 每层先求当前连通块重心；对每个儿子收集到重心的距离 `dis`，用全局结构维护已处理子树距离集合，`calc` 中先用已有集合回答，再把当前子树距离插入。
- Complexity: 若每层对节点做线性工作，总复杂度通常为 $O(n \log n)$；若每次对子树还要枚举询问，则需把询问因子单独算上。
- Pitfalls: 向下递归前必须在“未删去的当前子树”上重新求 `siz` 和重心；临时标记如 `exist/used` 必须按层清空，否则不同重心层会串答案。

## 点分树维护距离信息

- Model: 把点分治递归结构显式连成点分树；原树点到所有祖先重心的距离构成一条长度 $O(\log n)$ 的链，查询时沿链累加，更新时沿链打标。
- Invariant / correctness: 对任意查询点 `x` 和任意候选点 `y`，它们在点分树上的 `LCA` 对应某个经过的重心；把每个重心维护“整棵重心子树贡献”和“来自某个儿子方向的重复贡献”即可用容斥去重。
- Implementation: 先建原树 LCA 以支持 `dis(x, y)`；再建点分树 `fa2`；维护两类结构 `t1/t2`，查询时累加祖先 `t1` 并减去上一跳方向 `t2`，更新时同样双结构同步。
- Complexity: 建树 $O(n \log n)$；单次更新/查询通常 $O(\log^2 n)$，若底层结构更强可继续压常数。
- Pitfalls: 点分树重心递归时对子树重新求大小；`t2` 存的是“从父重心看某个儿子方向”的贡献，减错方向会重复统计；距离上界要和底层权值结构匹配。

## Boruvka 最小生成树

- Model: 把当前每个并查集连通块向外的最小出边同时加入，重复直到只剩一个连通块或无边可加。
- Invariant / correctness: 每个连通块选择的最小出边都满足 MST 割性质，必可安全加入；一次并行加入后连通块数至少减半。
- Implementation: 每轮扫所有未失效边，给每个连通块记录最小出边编号；之后枚举连通块代表，把仍然连接不同块的候选边并入并查集。
- Complexity: 共做 $O(\log n)$ 轮，每轮扫边 $O(m \alpha(n))$，总复杂度 $O(m \alpha(n) \log n)$。
- Pitfalls: 候选边按“块代表”记录，但真正合并前必须再次 `find` 校验两端是否已在同一块；图不连通时最后边数不足 `n - 1` 要特判无解。

## 2-SAT 缩点判定与构造

- Model: 每个命题拆成“假/真”两个点，约束转成蕴含边；对子图做 SCC 缩点。
- Invariant / correctness: 若 `i` 与 `i + n` 在同一 SCC，则命题与其否定可互相推出，必无解；否则按缩点 DAG 的拓扑关系给变量赋值即可。
- Implementation: 常见编号是 `i` 表示假、`i + n` 表示真；对子句 `(a or b)` 连 `not a -> b` 和 `not b -> a`；Tarjan 后比较 `belong[i]` 与 `belong[i + n]` 决定取值。
- Complexity: 建图和缩点总计 $O(n + m)$。
- Pitfalls: 字面量到编号的映射必须全程一致；Tarjan 编号与 DAG 拓扑序方向相反，使用 `belong` 大小构造答案时要先确认本实现的 SCC 出栈顺序定义。

## 线段树优化建图

- Model: 用两棵线段树把“点到区间”和“区间到点”都拆成 $O(\log n)$ 条边，区间内部可经 0 权树边传递到每个叶子。
- Invariant / correctness: 出树负责父到子 0 权下传，入树负责子到父 0 权上收；同一位置在两棵树的叶子互连后，任意点区间连边都能被分解成“点/区间节点 + $O(\log n)$ 线段树节点”的路径。
- Implementation: 建树时给每个原点绑定叶子编号；`v -> [l, r]` 连接到出树覆盖段，` [l, r] -> v` 从入树覆盖段连向目标点；最后在扩展图上跑最短路。
- Complexity: 建图点数、边数均为 $O(n \log n + q \log n)$，单次区间操作 $O(\log n)$。
- Pitfalls: 两棵树方向不能混；叶子间双向 0 权桥必须补齐；最短路起点要落在与操作定义一致的那棵树的叶子编号上。

# 数学

## 线性筛求欧拉函数

- Model: 利用 $\varphi(n) = n \prod_{p \mid n}(1 - \frac{1}{p})$，在线性筛过程中按“最小质因子第一次筛到”的方式递推。
- Invariant / correctness: 若 `i` 是质数，则 `phi[i] = i - 1`；若 `x = i * p` 且 `p` 为 `i` 的最小质因子：
  `i % p != 0` 时，`i` 与 `p` 互质，`phi[x] = phi[i] * (p - 1)`；
  `i % p == 0` 时，`x` 与 `i` 质因子集合相同，`phi[x] = phi[i] * p`。
- Implementation: 维护素数表 `pr` 和合数标记 `notp`；对每个 `i` 枚举素数 `p` 直到 `i * p > n`，命中 `i % p == 0` 后立刻 `break`。
- Complexity: 线性筛总复杂度 $O(n)$。
- Pitfalls: 第二种递推式是乘 `p` 不是乘 `phi[p]`；`break` 不能漏，否则会破坏“每个合数只被最小质因子筛一次”的线性复杂度。

# 数据结构

## 笛卡尔树：线性建树与区间最值分治

- Model: 以数组下标为 BST 键、数组值为小根或大根堆优先级；每个子树对应连续区间，其根是该区间的最小值或最大值。
- Invariant / correctness: 从左到右处理时，单调栈维护已处理前缀的右链。弹出违反堆性质的后缀并重连为当前节点的左子树，保留中序下标顺序，同时恢复堆性质。
- Implementation: 小根树弹出 `a[stk.back()] > a[i]` 的节点，剩余栈顶连向当前节点，最后弹出的节点成为当前节点左儿子；大根树反转比较号。凡是每次按区间 `argmin/argmax` 取点并递归划分的问题，递归树就是笛卡尔树，可直接在树上维护子树信息或做 DP。
- Complexity: 每个节点至多入栈、出栈一次，建树时间和空间均为 $O(n)$；后续一次树上 DP 为 $O(n)$。
- Pitfalls: 相等值必须固定 `>` 或 `>=` 的 tie-break；数组单调时树会退化成链，深递归需改为迭代遍历。
